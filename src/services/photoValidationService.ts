/**
 * Photo validation service.
 *
 * NOTE: This endpoint is NOT yet defined in the backend OpenAPI spec.
 * Add methods here once the spec is published. For now this class is
 * intentionally empty — the existing mock lives in `src/lib/validatePhoto.ts`
 * and should be migrated here when the real endpoint exists.
 */
export const photoValidationService = {
  // TODO: validate(missionId: number, body: { dataUrl: string }): Promise<{ valid: boolean; reason?: string }>
};
